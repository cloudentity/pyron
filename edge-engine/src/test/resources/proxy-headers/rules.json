{
  "rules": [
    {
      "default": {
        "targetService": "virtual"
      },
      "endpoints": [
        {
          "method": "GET",
          "pathPattern": "/proxy-headers",
          "requestPlugins": [
            {
              "name": "ping",
              "conf": {}
            }
          ]
        }
      ]
    }
  ]
}